import styled from 'styled-components';

const MediaDetailsStyles = styled.div`
  display: flex;
  padding: 20px;

  span {
    padding-right: 5px;
    font-weight: bold;
  }

  #error {
    color: red;
  }
`;

export default MediaDetailsStyles;
