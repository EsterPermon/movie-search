import styled from 'styled-components';

const StyledInput = styled.input`
  padding: 0 8px;
  font-weight: bold;
  border: none;
  border-radius: 6px;
  font-size: 16px;
  display: flex;
  flex-grow: 1;
  outline: none;
`;

export default StyledInput;
