import styled from 'styled-components';

const PageNavigatorStyles = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 10px;
  font-weight: bold;

  button {
    border-radius: 4px;
    margin: 10px;
  }
`;

export default PageNavigatorStyles;
